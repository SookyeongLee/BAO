<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="spring.bao.mapper.ReviewIF">

	
	<select id="getReview" resultType="spring.bao.beans.ReviewBean">
	<![CDATA[
		SELECT  FROM RV WHERE RV_WIRGCODE = #{rvCode} AND RV_WIHELPER = #{rvCode} 
		AND RV_STAR = #{rvStar} AND RV_COMMENT = #{rvComment}
    ]]>
	</select>
	<insert id ="isReview" >
	<![CDATA[
		INSERT INTO RV(RV_WIRQCODE, RV_WIHELPER, RV_STAR, RV_COMMENT) VALUES(#{rvCode},#{rvHelper},#{rvStar},#{rvComment})
	]]>
	</insert>



</mapper>
